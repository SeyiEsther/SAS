@{
    var current = ViewData["AdminActive"] as string;
}
<div class="admin-nav">
    <a class="chip @(current == "Departments" ? "active" : "")" href="/Admin/Departments">Departments</a>
    <a class="chip @(current == "Areas" ? "active" : "")" href="/Admin/Areas">Areas</a>
    <a class="chip @(current == "Shifts" ? "active" : "")" href="/Admin/Shifts">Shifts</a>
    <a class="chip @(current == "TaskLists" ? "active" : "")" href="/Admin/TaskLists">Task lists</a>
    <a class="chip @(current == "People" ? "active" : "")" href="/Admin/People">People &amp; roles</a>
</div>
